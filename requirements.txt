# Core dependencies for ACT-wholebody
# For LeRobot dependencies, use: pip install -e lerobot/

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
accelerate>=0.20.0

# Data Processing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
pyarrow>=12.0.0
h5py>=3.8.0

# Image Processing
opencv-python>=4.8.0
pillow>=10.0.0

# Hugging Face
huggingface-hub>=0.16.0
datasets>=2.14.0

# Training & Logging
wandb>=0.15.0
tensorboard>=2.13.0
tqdm>=4.65.0

# Utilities
pyyaml>=6.0
click>=8.1.0
einops>=0.6.1
packaging>=23.0

# ROS dependencies (if needed for data conversion)
# Note: ROS packages should be installed via apt-get or conda
# rosbag, rospy, sensor-msgs, etc. are typically system packages
